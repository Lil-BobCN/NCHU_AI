# Package Note

This zip is a cleaned source package for Git publishing.

Excluded intentionally:
- .git, virtual environments, node_modules, caches, logs, build outputs
- .env and local secret files
- SSH keys and temporary deployment files
- local session logs and server credential documents
- ad-hoc untracked server diagnostic scripts

Use .env.example as the environment variable template.
