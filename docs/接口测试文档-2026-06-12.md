# 学校 RAG 智能问答系统接口测试文档

生成日期：2026-06-12

## 1. 测试约定

- 默认后端地址：`http://127.0.0.1:8010`
- 统一接口前缀：`/api/v1`，根路径 `/` 除外。
- 受保护接口需请求头：`Authorization: Bearer $TOKEN`。
- 登录获取 token 示例：

```bash
TOKEN=$(curl -s -X POST http://127.0.0.1:8010/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | jq -r '.data.access_token')
```

- 统一成功响应：

```json
{"code":0,"message":"ok","data":{}}
```

- FastAPI 参数校验错误通常返回 HTTP 422；未登录或 token 无效返回 HTTP 401；业务不存在常见返回 HTTP 404。
- 文件流接口和 SSE 流式接口不使用统一响应包装。

## 2. 状态枚举与公共对象

### 2.1 文档状态

| 状态 | 含义 |
| --- | --- |
| uploaded | 已上传，尚未完成处理。 |
| parsed | 已解析，待切片或向量化。 |
| chunked | 已切片，待向量化。 |
| indexed | 已完成向量化，可参与检索。 |
| failed | 处理失败。 |
| needs_conversion | 旧版 Office 文件需先转换。 |
| needs_extraction | 压缩包需先解压导入。 |
| no_indexable_content | 解析完成但没有可入库正文。 |
| converted | 源文件已转换，请查看转换后的子文档。 |
| extracted | 压缩包已解压，请查看导入的子文档。 |
| deleted | 已软删除。 |

### 2.2 任务状态

| 状态 | 含义 |
| --- | --- |
| pending | 等待执行。 |
| running | 执行中。 |
| succeeded | 执行成功。 |
| failed | 执行失败。 |
| skipped | 因状态不满足被跳过。 |
| canceled | 已取消，目前代码仅保留枚举。 |

### 2.3 通用 Document 字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | uuid | 是 | 文档 ID。 |
| title | string | 是 | 展示标题；标题乱码时后端会回退为文件名。 |
| file_name | string | 是 | 原始文件名。 |
| file_ext | string | 是 | 文件扩展名，小写。 |
| file_size | integer | 是 | 文件大小，单位字节。 |
| status | string | 是 | 文档状态：uploaded、parsed、chunked、indexed、failed、needs_conversion、needs_extraction、no_indexable_content、converted、extracted、deleted。 |
| source_url | string|null | 否 | 外部来源 URL。 |
| preview_url | string|null | 否 | 可预览访问地址，通常为 /api/v1/files 代理地址。 |
| download_url | string|null | 否 | 下载访问地址。 |
| parse_quality_score | number | 是 | 解析质量分，0 表示无质量分。 |
| error_message | string|null | 否 | 失败原因。 |
| created_at | datetime|null | 否 | 创建时间，ISO 8601。 |
| updated_at | datetime|null | 否 | 更新时间，ISO 8601。 |

### 2.4 通用 Job 字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | uuid | 是 | 任务 ID。 |
| document_id | uuid | 否 | 关联文档 ID。 |
| job_type | string | 是 | 任务类型：parse、chunk、embed、convert_office、extract_import、qa_generate。 |
| status | string | 是 | 任务状态：pending、running、succeeded、failed、skipped、canceled。 |
| progress | integer | 是 | 进度百分比，0-100。 |
| message | string|null | 否 | 任务阶段说明。 |
| error_message | string|null | 否 | 失败信息。 |
| result | object | 是 | 任务结果扩展字段，如转换后的文档 ID、解压导入文档列表等。 |
| created_at | datetime|null | 否 | 任务创建时间。 |

## 3. 接口明细

### 3.1 根路径

- 方法：`GET`
- 路径：`/`
- 需要登录：否
- 说明：服务根路径，用于快速确认应用名称和 API 进程是否响应。

请求参数：

无

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| name | string | 是 | 应用名称。 |
| status | string | 是 | 固定 ok。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/
```

### 3.2 健康检查

- 方法：`GET`
- 路径：`/api/v1/health`
- 需要登录：否
- 说明：检查 API、PostgreSQL、Redis、MinIO 是否可用。

请求参数：

无

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.api | string | 是 | API 状态。 |
| data.postgres | string | 是 | PostgreSQL 状态，ok 或 error 信息。 |
| data.redis | string | 是 | Redis 状态，ok 或 error 信息。 |
| data.minio | string | 是 | MinIO 状态，ok 或 error 信息。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/api/v1/health
```

### 3.3 就绪检查

- 方法：`GET`
- 路径：`/api/v1/readiness`
- 需要登录：否
- 说明：与健康检查逻辑相同，可用于部署探针或测试环境依赖验证。

请求参数：

无

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.api | string | 是 | API 状态。 |
| data.postgres | string | 是 | PostgreSQL 状态。 |
| data.redis | string | 是 | Redis 状态。 |
| data.minio | string | 是 | MinIO 状态。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/api/v1/readiness
```

### 3.4 文件代理下载/预览

- 方法：`GET`
- 路径：`/api/v1/files/{bucket}/{object_key}`
- 需要登录：否
- 说明：从 MinIO 读取对象并通过后端代理返回文件流。对象 key 支持路径。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| bucket | path string | 是 | MinIO bucket 名，如 rag-documents。 |
| object_key | path string | 是 | 对象 key，需 URL 编码。 |
| download_name | query string | 否 | 传入时响应 Content-Disposition 下载文件名。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| Content-Type | header | 是 | 对象 MIME 类型，默认 application/octet-stream。 |
| Content-Length | header | 否 | 对象字节数。 |
| Content-Disposition | header | 否 | download_name 存在时返回附件下载头。 |
| body | binary stream | 是 | 文件内容。 |

调用示例：

```bash
curl -L "http://127.0.0.1:8010/api/v1/files/rag-documents/2026/06/demo.pdf?download_name=demo.pdf" -o demo.pdf
```

注意事项：

- 该接口不走统一 code/message/data 包装。
- 文件不存在或无权限时返回 HTTP 404，detail=file_not_found_or_inaccessible。

### 3.5 文件代理 HEAD

- 方法：`HEAD`
- 路径：`/api/v1/files/{bucket}/{object_key}`
- 需要登录：否
- 说明：只检查文件元信息，不下载文件体。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| bucket | path string | 是 | MinIO bucket 名。 |
| object_key | path string | 是 | 对象 key。 |
| download_name | query string | 否 | 下载文件名。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| HTTP 200 | status | 是 | 对象可访问。 |
| Content-Type | header | 是 | 对象 MIME 类型。 |
| Content-Length | header | 否 | 对象字节数。 |

调用示例：

```bash
curl -I "http://127.0.0.1:8010/api/v1/files/rag-documents/2026/06/demo.pdf"
```

注意事项：

- 该接口不返回响应体。

### 3.6 登录

- 方法：`POST`
- 路径：`/api/v1/auth/login`
- 需要登录：否
- 说明：管理员登录，成功返回 JWT token。后续受保护接口使用 Authorization: Bearer <token>。

请求参数：

无

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| username | string | 是 | 管理员账号，默认配置常见为 admin。 |
| password | string | 是 | 管理员密码。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.access_token | string | 是 | JWT 访问令牌。 |
| data.token_type | string | 是 | 固定 bearer。 |
| data.expires_in | integer | 是 | 有效期秒数，当前为 86400。 |
| data.admin.id | uuid | 是 | 管理员 ID。 |
| data.admin.username | string | 是 | 用户名。 |
| data.admin.display_name | string | 是 | 显示名。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

注意事项：

- 账号或密码错误返回 HTTP 401。

### 3.7 当前登录用户

- 方法：`GET`
- 路径：`/api/v1/auth/me`
- 需要登录：是
- 说明：获取当前 token 对应管理员信息。

请求参数：

无

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.id | uuid | 是 | 管理员 ID。 |
| data.username | string | 是 | 用户名。 |
| data.display_name | string | 是 | 显示名。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/api/v1/auth/me \
  -H "Authorization: Bearer $TOKEN"
```

### 3.8 上传文档

- 方法：`POST`
- 路径：`/api/v1/documents/upload`
- 需要登录：是
- 说明：上传文档到 MinIO 并创建文档记录。auto_process=true 时创建后台解析/切片/向量化流水线。

请求参数：

无

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| file | multipart file | 是 | 上传文件，最大 100MB。支持 pdf、doc、docx、xls、xlsx、xlsm、txt、md、jpg、jpeg、png、bmp、tif、tiff、zip、rar。 |
| title | form string | 否 | 文档标题，缺省使用文件名。 |
| source_url | form string | 否 | 外部来源 URL。 |
| auto_process | form boolean | 否 | 是否自动处理，默认 true。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.document | object | 是 | 文档对象，字段见通用 Document。 |
| data.jobs | array | 是 | 自动处理任务概要；auto_process=false 时为空数组。 |
| id | uuid | 是 | 文档 ID。 |
| title | string | 是 | 展示标题；标题乱码时后端会回退为文件名。 |
| file_name | string | 是 | 原始文件名。 |
| file_ext | string | 是 | 文件扩展名，小写。 |
| file_size | integer | 是 | 文件大小，单位字节。 |
| status | string | 是 | 文档状态：uploaded、parsed、chunked、indexed、failed、needs_conversion、needs_extraction、no_indexable_content、converted、extracted、deleted。 |
| source_url | string|null | 否 | 外部来源 URL。 |
| preview_url | string|null | 否 | 可预览访问地址，通常为 /api/v1/files 代理地址。 |
| download_url | string|null | 否 | 下载访问地址。 |
| parse_quality_score | number | 是 | 解析质量分，0 表示无质量分。 |
| error_message | string|null | 否 | 失败原因。 |
| created_at | datetime|null | 否 | 创建时间，ISO 8601。 |
| updated_at | datetime|null | 否 | 更新时间，ISO 8601。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/documents/upload \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@sample_contract.pdf" \
  -F "title=测试合同" \
  -F "auto_process=true"
```

注意事项：

- 不支持文件类型返回 HTTP 415。
- 超过 max_upload_size_mb 返回 HTTP 413。

### 3.9 文档列表

- 方法：`GET`
- 路径：`/api/v1/documents`
- 需要登录：是
- 说明：分页查询未删除文档。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| keyword | query string | 否 | 按 title 或 file_name 模糊搜索。 |
| status | query string | 否 | 按文档状态过滤。 |
| page | query integer | 否 | 页码，默认 1。 |
| page_size | query integer | 否 | 每页数量，默认 20。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| items | array | 是 | 当前页数据列表。 |
| page | integer | 是 | 当前页码。 |
| page_size | integer | 是 | 每页数量。 |
| total | integer | 是 | 符合条件的总记录数。 |
| id | uuid | 是 | 文档 ID。 |
| title | string | 是 | 展示标题；标题乱码时后端会回退为文件名。 |
| file_name | string | 是 | 原始文件名。 |
| file_ext | string | 是 | 文件扩展名，小写。 |
| file_size | integer | 是 | 文件大小，单位字节。 |
| status | string | 是 | 文档状态：uploaded、parsed、chunked、indexed、failed、needs_conversion、needs_extraction、no_indexable_content、converted、extracted、deleted。 |
| source_url | string|null | 否 | 外部来源 URL。 |
| preview_url | string|null | 否 | 可预览访问地址，通常为 /api/v1/files 代理地址。 |
| download_url | string|null | 否 | 下载访问地址。 |
| parse_quality_score | number | 是 | 解析质量分，0 表示无质量分。 |
| error_message | string|null | 否 | 失败原因。 |
| created_at | datetime|null | 否 | 创建时间，ISO 8601。 |
| updated_at | datetime|null | 否 | 更新时间，ISO 8601。 |

调用示例：

```bash
curl -s "http://127.0.0.1:8010/api/v1/documents?keyword=合同&status=indexed&page=1&page_size=10" \
  -H "Authorization: Bearer $TOKEN"
```

### 3.10 文档详情

- 方法：`GET`
- 路径：`/api/v1/documents/{document_id}`
- 需要登录：是
- 说明：获取单个文档详情、切片数量和最近 5 条任务。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| id | uuid | 是 | 文档 ID。 |
| title | string | 是 | 展示标题；标题乱码时后端会回退为文件名。 |
| file_name | string | 是 | 原始文件名。 |
| file_ext | string | 是 | 文件扩展名，小写。 |
| file_size | integer | 是 | 文件大小，单位字节。 |
| status | string | 是 | 文档状态：uploaded、parsed、chunked、indexed、failed、needs_conversion、needs_extraction、no_indexable_content、converted、extracted、deleted。 |
| source_url | string|null | 否 | 外部来源 URL。 |
| preview_url | string|null | 否 | 可预览访问地址，通常为 /api/v1/files 代理地址。 |
| download_url | string|null | 否 | 下载访问地址。 |
| parse_quality_score | number | 是 | 解析质量分，0 表示无质量分。 |
| error_message | string|null | 否 | 失败原因。 |
| created_at | datetime|null | 否 | 创建时间，ISO 8601。 |
| updated_at | datetime|null | 否 | 更新时间，ISO 8601。 |
| data.stats.chunk_count | integer | 是 | 文档切片数量。 |
| data.latest_jobs | array | 是 | 最近 5 条任务，字段见 Job。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID \
  -H "Authorization: Bearer $TOKEN"
```

注意事项：

- document_id 格式错误返回 HTTP 422；文档不存在返回 HTTP 404。

### 3.11 删除文档

- 方法：`DELETE`
- 路径：`/api/v1/documents/{document_id}`
- 需要登录：是
- 说明：软删除文档并尝试删除 MinIO 原始文件和解析结果文件。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.id | uuid | 是 | 文档 ID。 |
| data.status | string | 是 | 固定 deleted。 |

调用示例：

```bash
curl -s -X DELETE http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID \
  -H "Authorization: Bearer $TOKEN"
```

### 3.12 重新解析文档

- 方法：`POST`
- 路径：`/api/v1/documents/{document_id}/reparse`
- 需要登录：是
- 说明：读取原始文件并重新执行完整流水线：解析、切片、向量化。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.document_id | uuid | 是 | 文档 ID。 |
| data.status | string | 是 | pending。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID/reparse \
  -H "Authorization: Bearer $TOKEN"
```

### 3.13 重新切片并向量化

- 方法：`POST`
- 路径：`/api/v1/documents/{document_id}/rechunk`
- 需要登录：是
- 说明：基于已有解析结果重新切片，随后自动执行向量化。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.document_id | uuid | 是 | 文档 ID。 |
| data.status | string | 是 | pending 或 skipped。 |
| data.message | string | 否 | skipped 时返回跳过原因。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID/rechunk \
  -H "Authorization: Bearer $TOKEN"
```

### 3.14 重新向量化

- 方法：`POST`
- 路径：`/api/v1/documents/{document_id}/reembed`
- 需要登录：是
- 说明：清理当前文档 embedding 后重新生成向量。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.document_id | uuid | 是 | 文档 ID。 |
| data.status | string | 是 | pending 或 skipped。 |
| data.message | string | 否 | skipped 时返回跳过原因。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID/reembed \
  -H "Authorization: Bearer $TOKEN"
```

### 3.15 转换旧版 Office 文档

- 方法：`POST`
- 路径：`/api/v1/documents/{document_id}/convert-office`
- 需要登录：是
- 说明：将 .doc/.xls 转为新版 Office 文件并创建子文档，随后对子文档入库。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID，文件扩展名必须为 .doc 或 .xls。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.document_id | uuid | 是 | 源文档 ID。 |
| data.job_type | string | 是 | convert_office。 |
| data.status | string | 是 | pending。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID/convert-office \
  -H "Authorization: Bearer $TOKEN"
```

注意事项：

- 非 .doc/.xls 返回 HTTP 400。

### 3.16 解压导入压缩包

- 方法：`POST`
- 路径：`/api/v1/documents/{document_id}/extract-archive`
- 需要登录：是
- 说明：安全解压 .zip/.rar 中支持的内部文件，为每个内部文件创建新文档并继续处理。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID，文件扩展名必须为 .zip 或 .rar。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.document_id | uuid | 是 | 压缩包文档 ID。 |
| data.job_type | string | 是 | extract_import。 |
| data.status | string | 是 | pending。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID/extract-archive \
  -H "Authorization: Bearer $TOKEN"
```

注意事项：

- 非 .zip/.rar 返回 HTTP 400。

### 3.17 从文档生成 QA

- 方法：`POST`
- 路径：`/api/v1/documents/{document_id}/qa-pairs/generate`
- 需要登录：是
- 说明：基于文档切片后台生成 QA 对，并同步 QA embedding。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID。 |
| count | query integer | 否 | 生成数量，默认 10。 |
| auto_enable | query boolean | 否 | 是否直接启用，默认 true；false 时状态为 draft。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.document_id | uuid | 是 | 文档 ID。 |
| data.job_type | string | 是 | qa_generate。 |
| data.status | string | 是 | pending 或 skipped。 |

调用示例：

```bash
curl -s -X POST "http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID/qa-pairs/generate?count=5&auto_enable=true" \
  -H "Authorization: Bearer $TOKEN"
```

### 3.18 获取解析结果

- 方法：`GET`
- 路径：`/api/v1/documents/{document_id}/parse-result`
- 需要登录：是
- 说明：获取文档解析后的正文或 Markdown 内容。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID。 |
| format | query string | 否 | markdown 或 text，默认 markdown。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.document_id | uuid | 是 | 文档 ID。 |
| data.format | string | 是 | 请求的格式。 |
| data.content | string | 是 | 解析内容。 |
| data.quality_score | number | 是 | 解析质量分。 |
| data.parse_meta | object | 是 | 解析元数据。 |

调用示例：

```bash
curl -s "http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID/parse-result?format=markdown" \
  -H "Authorization: Bearer $TOKEN"
```

注意事项：

- 解析结果不存在返回 HTTP 404。

### 3.19 文档切片列表

- 方法：`GET`
- 路径：`/api/v1/documents/{document_id}/chunks`
- 需要登录：是
- 说明：分页查询某个文档的切片。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID。 |
| page | query integer | 否 | 页码，默认 1。 |
| page_size | query integer | 否 | 每页数量，默认 20。 |
| keyword | query string | 否 | 按切片 content 模糊搜索。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| items | array | 是 | 当前页数据列表。 |
| page | integer | 是 | 当前页码。 |
| page_size | integer | 是 | 每页数量。 |
| total | integer | 是 | 符合条件的总记录数。 |
| items[].id | uuid | 是 | 切片 ID。 |
| items[].chunk_no | integer | 是 | 切片序号。 |
| items[].chunk_type | string | 是 | 切片类型，如 text、table、parent。 |
| items[].content | string | 是 | 切片内容。 |
| items[].char_count | integer | 是 | 字符数。 |
| items[].token_count | integer|null | 否 | token 数。 |
| items[].page_start | integer|null | 否 | 起始页码。 |
| items[].page_end | integer|null | 否 | 结束页码。 |
| items[].section_path | string|null | 否 | 章节路径。 |
| items[].metadata | object | 是 | 切片元数据。 |
| items[].is_active | boolean | 是 | 是否参与检索。 |

调用示例：

```bash
curl -s "http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID/chunks?page=1&page_size=20&keyword=付款" \
  -H "Authorization: Bearer $TOKEN"
```

### 3.20 任务详情

- 方法：`GET`
- 路径：`/api/v1/jobs/{job_id}`
- 需要登录：是
- 说明：查询单个后台任务进度。优先从 Redis 读取缓存进度，缓存不存在时读取数据库。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| job_id | path uuid | 是 | 任务 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| id | uuid | 是 | 任务 ID。 |
| document_id | uuid | 否 | 关联文档 ID。 |
| job_type | string | 是 | 任务类型：parse、chunk、embed、convert_office、extract_import、qa_generate。 |
| status | string | 是 | 任务状态：pending、running、succeeded、failed、skipped、canceled。 |
| progress | integer | 是 | 进度百分比，0-100。 |
| message | string|null | 否 | 任务阶段说明。 |
| error_message | string|null | 否 | 失败信息。 |
| result | object | 是 | 任务结果扩展字段，如转换后的文档 ID、解压导入文档列表等。 |
| created_at | datetime|null | 否 | 任务创建时间。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/api/v1/jobs/$JOB_ID \
  -H "Authorization: Bearer $TOKEN"
```

### 3.21 文档任务列表

- 方法：`GET`
- 路径：`/api/v1/documents/{document_id}/jobs`
- 需要登录：是
- 说明：查询某个文档的所有后台任务，按创建时间倒序。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| document_id | path uuid | 是 | 文档 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| id | uuid | 是 | 任务 ID。 |
| document_id | uuid | 否 | 关联文档 ID。 |
| job_type | string | 是 | 任务类型：parse、chunk、embed、convert_office、extract_import、qa_generate。 |
| status | string | 是 | 任务状态：pending、running、succeeded、failed、skipped、canceled。 |
| progress | integer | 是 | 进度百分比，0-100。 |
| message | string|null | 否 | 任务阶段说明。 |
| error_message | string|null | 否 | 失败信息。 |
| result | object | 是 | 任务结果扩展字段，如转换后的文档 ID、解压导入文档列表等。 |
| created_at | datetime|null | 否 | 任务创建时间。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/api/v1/documents/$DOCUMENT_ID/jobs \
  -H "Authorization: Bearer $TOKEN"
```

### 3.22 QA 列表

- 方法：`GET`
- 路径：`/api/v1/qa-pairs`
- 需要登录：是
- 说明：分页查询 QA 对。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| keyword | query string | 否 | 按 question 或 answer 模糊搜索。 |
| status | query string | 否 | 按 QA 状态过滤。 |
| document_id | query uuid | 否 | 按来源文档过滤。 |
| page | query integer | 否 | 页码，默认 1。 |
| page_size | query integer | 否 | 每页数量，默认 20。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| items | array | 是 | 当前页数据列表。 |
| page | integer | 是 | 当前页码。 |
| page_size | integer | 是 | 每页数量。 |
| total | integer | 是 | 符合条件的总记录数。 |
| id | uuid | 是 | QA ID。 |
| question | string | 是 | 问题。 |
| answer | string | 是 | 答案。 |
| status | string | 是 | QA 状态，常用 enabled、disabled、draft。 |
| source_document_id | uuid|null | 否 | 来源文档 ID。 |
| source_chunk_ids | uuid[] | 否 | 来源切片 ID 列表。 |
| source_url | string|null | 否 | 来源 URL。 |
| tags | string[] | 否 | 标签。 |
| version | integer | 是 | 版本号，更新时递增。 |
| updated_at | datetime|null | 否 | 更新时间。 |

调用示例：

```bash
curl -s "http://127.0.0.1:8010/api/v1/qa-pairs?keyword=报名&status=enabled&page=1&page_size=10" \
  -H "Authorization: Bearer $TOKEN"
```

### 3.23 创建 QA

- 方法：`POST`
- 路径：`/api/v1/qa-pairs`
- 需要登录：是
- 说明：手动创建 QA 对，并在后台同步 QA embedding。

请求参数：

无

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| question | string | 是 | 问题。 |
| answer | string | 是 | 答案。 |
| status | string | 否 | 默认 enabled。 |
| source_document_id | uuid|null | 否 | 来源文档 ID。 |
| source_chunk_ids | uuid[]|null | 否 | 来源切片 ID。 |
| tags | string[]|null | 否 | 标签。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| id | uuid | 是 | QA ID。 |
| question | string | 是 | 问题。 |
| answer | string | 是 | 答案。 |
| status | string | 是 | QA 状态，常用 enabled、disabled、draft。 |
| source_document_id | uuid|null | 否 | 来源文档 ID。 |
| source_chunk_ids | uuid[] | 否 | 来源切片 ID 列表。 |
| source_url | string|null | 否 | 来源 URL。 |
| tags | string[] | 否 | 标签。 |
| version | integer | 是 | 版本号，更新时递增。 |
| updated_at | datetime|null | 否 | 更新时间。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/qa-pairs \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"question":"报名时间是什么时候？","answer":"以通知文件为准。","status":"enabled","tags":["测试"]}'
```

### 3.24 更新 QA

- 方法：`PUT`
- 路径：`/api/v1/qa-pairs/{qa_pair_id}`
- 需要登录：是
- 说明：更新 QA 内容、状态或标签，版本号递增，并后台同步 embedding。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| qa_pair_id | path uuid | 是 | QA ID。 |

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| question | string|null | 否 | 新问题。 |
| answer | string|null | 否 | 新答案。 |
| status | string|null | 否 | 新状态。 |
| tags | string[]|null | 否 | 新标签。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| id | uuid | 是 | QA ID。 |
| question | string | 是 | 问题。 |
| answer | string | 是 | 答案。 |
| status | string | 是 | QA 状态，常用 enabled、disabled、draft。 |
| source_document_id | uuid|null | 否 | 来源文档 ID。 |
| source_chunk_ids | uuid[] | 否 | 来源切片 ID 列表。 |
| source_url | string|null | 否 | 来源 URL。 |
| tags | string[] | 否 | 标签。 |
| version | integer | 是 | 版本号，更新时递增。 |
| updated_at | datetime|null | 否 | 更新时间。 |

调用示例：

```bash
curl -s -X PUT http://127.0.0.1:8010/api/v1/qa-pairs/$QA_PAIR_ID \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"answer":"更新后的答案","tags":["回归测试"]}'
```

### 3.25 删除 QA

- 方法：`DELETE`
- 路径：`/api/v1/qa-pairs/{qa_pair_id}`
- 需要登录：是
- 说明：软删除 QA：设置 deleted_at，并将 status 改为 disabled。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| qa_pair_id | path uuid | 是 | QA ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.id | uuid | 是 | QA ID。 |

调用示例：

```bash
curl -s -X DELETE http://127.0.0.1:8010/api/v1/qa-pairs/$QA_PAIR_ID \
  -H "Authorization: Bearer $TOKEN"
```

### 3.26 更新 QA 状态

- 方法：`PATCH`
- 路径：`/api/v1/qa-pairs/{qa_pair_id}/status`
- 需要登录：是
- 说明：只更新 QA 状态。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| qa_pair_id | path uuid | 是 | QA ID。 |

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| status | string | 是 | 目标状态，如 enabled、disabled、draft。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| id | uuid | 是 | QA ID。 |
| question | string | 是 | 问题。 |
| answer | string | 是 | 答案。 |
| status | string | 是 | QA 状态，常用 enabled、disabled、draft。 |
| source_document_id | uuid|null | 否 | 来源文档 ID。 |
| source_chunk_ids | uuid[] | 否 | 来源切片 ID 列表。 |
| source_url | string|null | 否 | 来源 URL。 |
| tags | string[] | 否 | 标签。 |
| version | integer | 是 | 版本号，更新时递增。 |
| updated_at | datetime|null | 否 | 更新时间。 |

调用示例：

```bash
curl -s -X PATCH http://127.0.0.1:8010/api/v1/qa-pairs/$QA_PAIR_ID/status \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"status":"disabled"}'
```

### 3.27 创建会话

- 方法：`POST`
- 路径：`/api/v1/conversations`
- 需要登录：是
- 说明：创建一个聊天会话。

请求参数：

无

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| title | string | 否 | 会话标题，默认“新的对话”。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| id | uuid | 是 | 会话 ID。 |
| title | string | 是 | 会话标题。 |
| summary | string | 是 | 会话摘要。 |
| message_count | integer | 是 | 消息数量。 |
| last_message_at | datetime|null | 否 | 最后消息时间。 |
| created_at | datetime|null | 否 | 创建时间。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/conversations \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"测试会话"}'
```

### 3.28 会话列表

- 方法：`GET`
- 路径：`/api/v1/conversations`
- 需要登录：是
- 说明：分页查询未删除会话。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| page | query integer | 否 | 页码，默认 1。 |
| page_size | query integer | 否 | 每页数量，默认 20。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| items | array | 是 | 当前页数据列表。 |
| page | integer | 是 | 当前页码。 |
| page_size | integer | 是 | 每页数量。 |
| total | integer | 是 | 符合条件的总记录数。 |
| id | uuid | 是 | 会话 ID。 |
| title | string | 是 | 会话标题。 |
| summary | string | 是 | 会话摘要。 |
| message_count | integer | 是 | 消息数量。 |
| last_message_at | datetime|null | 否 | 最后消息时间。 |
| created_at | datetime|null | 否 | 创建时间。 |

调用示例：

```bash
curl -s "http://127.0.0.1:8010/api/v1/conversations?page=1&page_size=20" \
  -H "Authorization: Bearer $TOKEN"
```

### 3.29 会话消息列表

- 方法：`GET`
- 路径：`/api/v1/conversations/{conversation_id}/messages`
- 需要登录：是
- 说明：按时间正序查询某个会话的消息。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| conversation_id | path uuid | 是 | 会话 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| id | uuid | 是 | 消息 ID。 |
| conversation_id | uuid | 是 | 所属会话 ID。 |
| role | string | 是 | 消息角色：user 或 assistant。 |
| content | string | 是 | 消息正文。 |
| rewritten_query | string|null | 否 | 用于检索的改写查询。 |
| retrieval_trace | object | 是 | 检索过程追踪。 |
| citations | array | 是 | 引用来源列表。 |
| suggested_questions | array | 是 | 推荐追问列表。 |
| created_at | datetime|null | 否 | 创建时间。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/api/v1/conversations/$CONVERSATION_ID/messages \
  -H "Authorization: Bearer $TOKEN"
```

### 3.30 删除会话

- 方法：`DELETE`
- 路径：`/api/v1/conversations/{conversation_id}`
- 需要登录：是
- 说明：软删除会话。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| conversation_id | path uuid | 是 | 会话 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.id | uuid | 是 | 会话 ID。 |

调用示例：

```bash
curl -s -X DELETE http://127.0.0.1:8010/api/v1/conversations/$CONVERSATION_ID \
  -H "Authorization: Bearer $TOKEN"
```

### 3.31 检索调试搜索

- 方法：`POST`
- 路径：`/api/v1/retrieval/search`
- 需要登录：是
- 说明：执行混合检索：向量召回、关键词召回、QA 召回、RRF 融合、rerank 和上下文扩展。

请求参数：

无

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| query | string | 是 | 检索问题。 |
| top_k | integer | 否 | 召回数量，默认 30，最大 100。 |
| rerank_top_k | integer | 否 | 最终返回数量，默认 5，最大 50。 |
| document_ids | uuid[]|null | 否 | 限定文档 ID。 |
| enable_rewrite | boolean | 否 | 当前接口定义包含该字段，但 search 实现未使用。 |
| enable_qa_recall | boolean | 否 | 是否启用 QA 召回，默认 true。 |
| enable_keyword_recall | boolean | 否 | 是否启用关键词召回，默认 true。 |
| enable_vector_recall | boolean | 否 | 是否启用向量召回，默认 true。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.raw_query | string | 是 | 原始查询。 |
| data.rewritten_query | string | 是 | 当前同 raw_query。 |
| data.recall_results | array | 是 | 召回结果。 |
| data.rerank_results | array | 是 | rerank 后结果。 |
| data.final_context | array | 是 | 扩展后的最终上下文。 |
| data.answer_context | array | 是 | 用于回答的精选上下文。 |
| data.citations | array | 是 | 引用来源。 |
| data.retrieval_options | object | 是 | 本次检索参数。 |
| data.effective_settings | object | 是 | 生效的 RAG 设置。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/retrieval/search \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"query":"报名与缴费是怎样的？","top_k":30,"rerank_top_k":5}'
```

### 3.32 检索日志列表

- 方法：`GET`
- 路径：`/api/v1/retrieval/logs`
- 需要登录：是
- 说明：分页查询检索日志概要。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| keyword | query string | 否 | 按 raw_query 模糊搜索。 |
| page | query integer | 否 | 页码，默认 1。 |
| page_size | query integer | 否 | 每页数量，默认 20。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| items | array | 是 | 当前页数据列表。 |
| page | integer | 是 | 当前页码。 |
| page_size | integer | 是 | 每页数量。 |
| total | integer | 是 | 符合条件的总记录数。 |
| items[].id | uuid | 是 | 日志 ID。 |
| items[].raw_query | string | 是 | 原始问题。 |
| items[].rewritten_query | string|null | 否 | 改写后问题。 |
| items[].latency_ms | integer|null | 否 | 耗时毫秒。 |
| items[].created_at | datetime|null | 否 | 创建时间。 |

调用示例：

```bash
curl -s "http://127.0.0.1:8010/api/v1/retrieval/logs?keyword=报名&page=1&page_size=20" \
  -H "Authorization: Bearer $TOKEN"
```

### 3.33 检索日志详情

- 方法：`GET`
- 路径：`/api/v1/retrieval/logs/{log_id}`
- 需要登录：是
- 说明：查询单条检索日志完整追踪。不存在时 code=0 且 data=null。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| log_id | path uuid | 是 | 日志 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.id | uuid | 是 | 日志 ID。 |
| data.raw_query | string | 是 | 原始问题。 |
| data.rewritten_query | string|null | 否 | 改写问题。 |
| data.recall_results | array | 是 | 召回结果。 |
| data.rerank_results | array | 是 | 排序结果。 |
| data.final_context | array | 是 | 最终上下文。 |
| data.citations | array | 是 | 引用。 |
| data.suggested_questions | array | 是 | 推荐追问。 |
| data.answer | string|null | 否 | 回答文本。 |
| data.latency_ms | integer|null | 否 | 耗时毫秒。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/api/v1/retrieval/logs/$LOG_ID \
  -H "Authorization: Bearer $TOKEN"
```

### 3.34 流式问答

- 方法：`POST`
- 路径：`/api/v1/chat/stream`
- 需要登录：是
- 说明：SSE 流式问答。会创建或复用会话，写入用户消息、助手消息和检索日志。

请求参数：

无

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| conversation_id | uuid|null | 否 | 会话 ID；为空时自动创建。 |
| question | string | 是 | 用户问题。 |
| top_k | integer | 否 | 召回数量，默认 8。 |
| rerank_top_k | integer | 否 | 最终上下文数量，默认 5。 |
| document_ids | uuid[]|null | 否 | 限定文档。 |
| enable_rewrite | boolean | 否 | 是否允许结合历史改写追问，默认 true。 |
| enable_suggested_questions | boolean | 否 | 是否生成推荐追问，默认 true。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| event: message_start | SSE | 是 | 返回 conversation_id、user_message_id、assistant_message_id 等。 |
| event: retrieval_start | SSE | 是 | 检索开始。 |
| event: retrieval | SSE | 是 | 召回和排序数量。 |
| event: retrieval_done | SSE | 是 | 检索完成。 |
| event: answer_cache | SSE | 否 | 命中答案缓存时返回。 |
| event: delta | SSE | 是 | 回答增量文本，data.content。 |
| event: citations | SSE | 是 | 引用来源。 |
| event: suggested_questions | SSE | 是 | 推荐追问。 |
| event: message_end | SSE | 是 | 回答结束，含 latency_ms。 |

调用示例：

```bash
curl -N -X POST http://127.0.0.1:8010/api/v1/chat/stream \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"question":"辅修学士学位报名与缴费是怎样的？","enable_suggested_questions":false}'
```

注意事项：

- 该接口 media_type 为 text/event-stream，不使用统一响应包装。

### 3.35 非流式问答

- 方法：`POST`
- 路径：`/api/v1/chat`
- 需要登录：是
- 说明：一次性返回完整回答、引用、推荐追问和检索追踪。

请求参数：

无

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| conversation_id | uuid|null | 否 | 会话 ID；为空时自动创建。 |
| question | string | 是 | 用户问题。 |
| top_k | integer | 否 | 召回数量，默认 8。 |
| rerank_top_k | integer | 否 | 最终上下文数量，默认 5。 |
| document_ids | uuid[]|null | 否 | 限定文档。 |
| enable_rewrite | boolean | 否 | 是否允许历史改写，默认 true。 |
| enable_suggested_questions | boolean | 否 | 是否生成推荐追问，默认 true。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.answer | string | 是 | 完整回答。 |
| data.citations | array | 是 | 引用来源。 |
| data.suggested_questions | array | 是 | 推荐追问。 |
| data.retrieval_trace | object | 是 | 检索追踪。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/chat \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"question":"辅修学士学位报名与缴费是怎样的？","top_k":8,"rerank_top_k":5}'
```

### 3.36 获取 RAG 设置

- 方法：`GET`
- 路径：`/api/v1/settings/rag`
- 需要登录：是
- 说明：获取当前生效的 RAG 参数。

请求参数：

无

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.chunk_size | integer | 是 | 普通文本切片长度。 |
| data.chunk_overlap | integer | 是 | 切片重叠长度。 |
| data.table_chunk_rows | integer | 是 | 表格分块行数。 |
| data.table_full_index_max_rows | integer | 是 | 表格全量索引最大行数。 |
| data.table_sample_rows | integer | 是 | 表格抽样行数。 |
| data.max_document_chunks | integer | 是 | 单文档最大切片数。 |
| data.max_embedding_chars_per_document | integer | 是 | 单文档最大向量化字符数。 |
| data.vector_top_k | integer | 是 | 向量召回数。 |
| data.keyword_top_k | integer | 是 | 关键词召回数。 |
| data.qa_top_k | integer | 是 | QA 召回数。 |
| data.rerank_top_k | integer | 是 | rerank 返回数。 |
| data.similarity_threshold | number | 是 | 相似度阈值。 |
| data.rerank_threshold | number | 是 | rerank 阈值。 |
| data.rerank_enabled | boolean | 是 | 是否启用 rerank。 |
| data.rerank_max_candidates | integer | 是 | rerank 最大候选数量。 |
| data.persisted | boolean | 是 | 固定 true。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/api/v1/settings/rag \
  -H "Authorization: Bearer $TOKEN"
```

### 3.37 更新 RAG 设置

- 方法：`PUT`
- 路径：`/api/v1/settings/rag`
- 需要登录：是
- 说明：保存 RAG 参数到 .cache/rag_settings.json，并返回当前生效值。

请求参数：

无

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| chunk_size | integer | 是 | 普通文本切片长度。 |
| chunk_overlap | integer | 是 | 切片重叠长度。 |
| table_chunk_rows | integer | 否 | 默认 50。 |
| table_full_index_max_rows | integer | 否 | 默认 500。 |
| table_sample_rows | integer | 否 | 默认 50。 |
| max_document_chunks | integer | 否 | 默认 300。 |
| max_embedding_chars_per_document | integer | 否 | 默认 200000。 |
| vector_top_k | integer | 是 | 向量召回数。 |
| keyword_top_k | integer | 是 | 关键词召回数。 |
| qa_top_k | integer | 是 | QA 召回数。 |
| rerank_top_k | integer | 是 | rerank 返回数。 |
| similarity_threshold | number | 是 | 相似度阈值。 |
| rerank_threshold | number | 是 | rerank 阈值。 |
| rerank_enabled | boolean | 否 | 默认 true。 |
| rerank_max_candidates | integer | 否 | 默认 10。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data | object | 是 | 字段同 GET /settings/rag。 |

调用示例：

```bash
curl -s -X PUT http://127.0.0.1:8010/api/v1/settings/rag \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"chunk_size":800,"chunk_overlap":120,"table_chunk_rows":50,"table_full_index_max_rows":500,"table_sample_rows":50,"max_document_chunks":300,"max_embedding_chars_per_document":200000,"vector_top_k":30,"keyword_top_k":30,"qa_top_k":10,"rerank_top_k":5,"similarity_threshold":0.35,"rerank_threshold":0.45,"rerank_enabled":true,"rerank_max_candidates":10}'
```

### 3.38 评测用例列表

- 方法：`GET`
- 路径：`/api/v1/evaluation/cases`
- 需要登录：是
- 说明：分页查询评测用例。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| page | query integer | 否 | 页码，默认 1。 |
| page_size | query integer | 否 | 每页数量，默认 20。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| items | array | 是 | 当前页数据列表。 |
| page | integer | 是 | 当前页码。 |
| page_size | integer | 是 | 每页数量。 |
| total | integer | 是 | 符合条件的总记录数。 |
| items[].id | uuid | 是 | 用例 ID。 |
| items[].question | string | 是 | 测试问题。 |
| items[].expected_answer | string|null | 否 | 期望答案或关键词。 |
| items[].expected_document_ids | uuid[] | 否 | 期望命中文档。 |
| items[].expected_chunk_ids | uuid[] | 否 | 期望命中切片。 |
| items[].tags | string[] | 否 | 标签。 |
| items[].difficulty | string|null | 否 | 难度。 |
| items[].is_active | boolean | 是 | 是否启用。 |
| items[].created_at | datetime|null | 否 | 创建时间。 |

调用示例：

```bash
curl -s "http://127.0.0.1:8010/api/v1/evaluation/cases?page=1&page_size=20" \
  -H "Authorization: Bearer $TOKEN"
```

### 3.39 创建评测用例

- 方法：`POST`
- 路径：`/api/v1/evaluation/cases`
- 需要登录：是
- 说明：创建 RAG 评测用例。

请求参数：

无

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| question | string | 是 | 测试问题。 |
| expected_answer | string|null | 否 | 期望答案或关键词。 |
| expected_document_ids | uuid[]|null | 否 | 期望命中文档 ID。 |
| expected_chunk_ids | uuid[]|null | 否 | 期望命中切片 ID。 |
| tags | string[]|null | 否 | 标签。 |
| difficulty | string|null | 否 | 难度。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.id | uuid | 是 | 用例 ID。 |
| data.question | string | 是 | 问题。 |
| data.expected_answer | string|null | 否 | 期望答案。 |
| data.expected_document_ids | uuid[] | 否 | 期望文档。 |
| data.expected_chunk_ids | uuid[] | 否 | 期望切片。 |
| data.tags | string[] | 否 | 标签。 |
| data.difficulty | string|null | 否 | 难度。 |
| data.is_active | boolean | 是 | 是否启用。 |
| data.created_at | datetime|null | 否 | 创建时间。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/evaluation/cases \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"question":"报名与缴费是怎样的？","expected_answer":"报名,缴费","tags":["回归"],"difficulty":"normal"}'
```

### 3.40 创建评测批次

- 方法：`POST`
- 路径：`/api/v1/evaluation/runs`
- 需要登录：是
- 说明：创建后台评测批次，异步执行用例并计算指标。

请求参数：

无

请求体：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| name | string | 是 | 批次名称。 |
| case_ids | uuid[]|null | 否 | 指定用例 ID；为空则执行所有 active 用例。 |
| config | object | 否 | 评测配置，默认空对象。 |

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.run_id | uuid | 是 | 评测批次 ID。 |
| data.status | string | 是 | running。 |

调用示例：

```bash
curl -s -X POST http://127.0.0.1:8010/api/v1/evaluation/runs \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name":"回归评测-2026-06-12","case_ids":[],"config":{"top_k":8}}'
```

### 3.41 评测批次详情

- 方法：`GET`
- 路径：`/api/v1/evaluation/runs/{run_id}`
- 需要登录：是
- 说明：查询评测批次状态、指标和结果列表。

请求参数：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| run_id | path uuid | 是 | 评测批次 ID。 |

请求体：

无

响应字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | integer | 是 | 统一业务状态码，成功固定为 0。 |
| message | string | 是 | 统一响应消息，成功默认 ok。 |
| data | any | 否 | 接口实际数据；不同接口结构不同。 |
| data.id | uuid | 是 | 批次 ID。 |
| data.name | string | 是 | 批次名称。 |
| data.status | string | 是 | running、succeeded 或 failed。 |
| data.metrics | object | 是 | 聚合指标，如 case_count、top3_hit_rate、answer_score_avg。 |
| data.results | array | 是 | 评测结果列表。 |
| results[].case_id | uuid | 是 | 用例 ID。 |
| results[].answer | string|null | 否 | 实际回答。 |
| results[].hit_top3 | boolean|null | 否 | Top3 是否命中期望来源。 |
| results[].hit_top5 | boolean|null | 否 | Top5 是否命中期望来源。 |
| results[].answer_score | number|null | 否 | 答案关键词得分。 |
| results[].citation_score | number|null | 否 | 引用得分。 |
| results[].suggested_question_score | number|null | 否 | 推荐追问得分。 |
| results[].failure_reason | string|null | 否 | 失败原因。 |
| results[].trace | object | 是 | 评测追踪。 |

调用示例：

```bash
curl -s http://127.0.0.1:8010/api/v1/evaluation/runs/$RUN_ID \
  -H "Authorization: Bearer $TOKEN"
```
