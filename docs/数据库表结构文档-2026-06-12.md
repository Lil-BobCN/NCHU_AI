# 学校 RAG 智能问答系统数据库表结构文档

生成日期：2026-06-12

## 1. 数据库概览

- 数据库：PostgreSQL。
- 主要扩展：pgvector、pg_trgm、uuid-ossp。
- 向量维度：1024，表 `chunk_embeddings` 和 `qa_pair_embeddings` 使用 HNSW 索引。
- 本文档依据当前 `backend/sql/001_init.sql` 生成。

扩展清单：

| 扩展 |
| --- |
| vector |
| pg_trgm |
| "uuid-ossp" |

## 2. 表关系概要

| 表名 | 说明 |
| --- | --- |
| admins | 管理员账号表。 |
| documents | 文档主表，保存文件元数据、存储位置和处理状态。 |
| document_jobs | 文档后台任务表，记录解析、切片、向量化、转换、解压、QA 生成等任务。 |
| document_parse_results | 文档解析结果表，一篇文档最多一条解析结果。 |
| document_chunks | 文档切片表，保存可检索文本片段和父子切片关系。 |
| chunk_embeddings | 文档切片向量表，使用 pgvector 存储 1024 维 embedding。 |
| qa_pairs | 人工或自动生成的问答对表。 |
| qa_pair_embeddings | QA 对向量表，使用 pgvector 存储 1024 维 embedding。 |
| conversations | 聊天会话表。 |
| conversation_messages | 聊天消息表，保存问答内容、引用和检索追踪。 |
| retrieval_logs | 检索与回答日志表，用于调试和评估 RAG 效果。 |
| evaluation_cases | 评测用例表。 |
| evaluation_runs | 评测批次表。 |
| evaluation_results | 评测结果明细表。 |

## 3. 表字段明细

### 3.1 admins

管理员账号表。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| username | varchar(64) | 否 |  | 唯一 | 管理员登录账号。 |
| password_hash | varchar(255) | 否 |  |  | 管理员密码哈希。 |
| display_name | varchar(64) | 否 |  |  | 管理员显示名称。 |
| is_active | boolean | 否 | true |  | 是否启用。 |
| last_login_at | timestamptz | 是 |  |  | 最后登录时间。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |
| updated_at | timestamptz | 否 | now() |  | 更新时间。 |

索引：

无

### 3.2 documents

文档主表，保存文件元数据、存储位置和处理状态。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| title | varchar(255) | 否 |  |  | 标题。 |
| file_name | varchar(255) | 否 |  |  | 原始文件名。 |
| file_ext | varchar(32) | 否 |  |  | 文件扩展名。 |
| mime_type | varchar(128) | 是 |  |  | MIME 类型。 |
| file_size | bigint | 否 |  |  | 文件大小，单位字节。 |
| file_hash | varchar(128) | 否 |  |  | 文件 SHA256 哈希。 |
| storage_bucket | varchar(128) | 否 |  |  | MinIO bucket。 |
| storage_object_key | varchar(512) | 否 |  |  | MinIO object key。 |
| source_url | text | 是 |  |  | 外部来源 URL。 |
| preview_url | text | 是 |  |  | 预览 URL。 |
| download_url | text | 是 |  |  | 下载 URL。 |
| status | varchar(32) | 否 | 'uploaded' |  | 文档状态，见接口文档状态枚举。 |
| parse_quality_score | numeric(5,2) | 是 |  |  | 解析质量分。 |
| error_message | text | 是 |  |  | 错误信息。 |
| created_by | uuid | 是 |  |  | 创建人管理员 ID。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |
| updated_at | timestamptz | 否 | now() |  | 更新时间。 |
| deleted_at | timestamptz | 是 |  |  | 软删除时间，非空表示已删除。 |

索引：

无

### 3.3 document_jobs

文档后台任务表，记录解析、切片、向量化、转换、解压、QA 生成等任务。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| document_id | uuid | 否 |  | 外键 documents(id) | 关联文档 ID。 |
| job_type | varchar(32) | 否 |  |  | 任务类型。 |
| status | varchar(32) | 否 | 'pending' |  | 任务状态：pending、running、succeeded、failed、skipped、canceled。 |
| progress | integer | 否 | 0 |  | 任务进度百分比。 |
| message | text | 是 |  |  | 任务消息。 |
| error_message | text | 是 |  |  | 错误信息。 |
| retry_count | integer | 否 | 0 |  | 重试次数。 |
| params | jsonb | 否 | '{}' |  | 任务参数 JSON。 |
| result | jsonb | 否 | '{}' |  | 任务结果 JSON。 |
| started_at | timestamptz | 是 |  |  | 任务开始时间。 |
| finished_at | timestamptz | 是 |  |  | 任务结束时间。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |
| updated_at | timestamptz | 否 | now() |  | 更新时间。 |

索引：

无

### 3.4 document_parse_results

文档解析结果表，一篇文档最多一条解析结果。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| document_id | uuid | 否 |  | 唯一；外键 documents(id) | 关联文档 ID。 |
| parser_name | varchar(64) | 否 |  |  | 解析器名称。 |
| parser_version | varchar(64) | 是 |  |  | 解析器版本。 |
| content_text | text | 否 |  |  | 解析纯文本。 |
| content_md | text | 是 |  |  | 解析 Markdown。 |
| content_object_key | varchar(512) | 是 |  |  | 解析内容在 MinIO 中的 object key。 |
| page_count | integer | 是 |  |  | 页数。 |
| quality_score | numeric(5,2) | 是 |  |  | 质量分。 |
| parse_meta | jsonb | 否 | '{}' |  | 解析元数据 JSON。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |
| updated_at | timestamptz | 否 | now() |  | 更新时间。 |

索引：

无

### 3.5 document_chunks

文档切片表，保存可检索文本片段和父子切片关系。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| document_id | uuid | 否 |  | 外键 documents(id) | 关联文档 ID。 |
| parent_chunk_id | uuid | 是 |  | 外键 document_chunks(id) | 父切片 ID。 |
| chunk_no | integer | 否 |  |  | 切片序号。 |
| chunk_type | varchar(32) | 否 | 'text' |  | 切片类型。 |
| content | text | 否 |  |  | 文本内容。 |
| content_hash | varchar(128) | 否 |  |  | 内容哈希。 |
| token_count | integer | 是 |  |  | token 数。 |
| char_count | integer | 否 |  |  | 字符数。 |
| page_start | integer | 是 |  |  | 起始页码。 |
| page_end | integer | 是 |  |  | 结束页码。 |
| section_path | text | 是 |  |  | 章节路径。 |
| metadata | jsonb | 否 | '{}' |  | 元数据 JSON。 |
| search_vector | tsvector | 是 |  |  | PostgreSQL 全文检索向量。 |
| is_active | boolean | 否 | true |  | 是否启用。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |
| updated_at | timestamptz | 否 | now() |  | 更新时间。 |

索引：

| 索引定义 |
| --- |
| CREATE INDEX IF NOT EXISTS idx_chunks_search_vector ON document_chunks USING GIN(search_vector); |
| CREATE INDEX IF NOT EXISTS idx_chunks_metadata ON document_chunks USING GIN(metadata); |

### 3.6 chunk_embeddings

文档切片向量表，使用 pgvector 存储 1024 维 embedding。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| chunk_id | uuid | 否 |  | 唯一；外键 document_chunks(id) |  |
| document_id | uuid | 否 |  | 外键 documents(id) | 关联文档 ID。 |
| embedding | vector(1024) | 否 |  |  | pgvector 向量。 |
| embedding_model | varchar(128) | 否 |  |  | 向量模型名称。 |
| embedding_provider | varchar(64) | 否 |  |  | 向量提供方。 |
| content_hash | varchar(128) | 否 |  |  | 内容哈希。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |
| updated_at | timestamptz | 否 | now() |  | 更新时间。 |

索引：

| 索引定义 |
| --- |
| CREATE INDEX IF NOT EXISTS idx_chunk_embeddings_vector_hnsw ON chunk_embeddings USING hnsw (embedding vector_cosine_ops); |

### 3.7 qa_pairs

人工或自动生成的问答对表。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| question | text | 否 |  |  | 问题。 |
| answer | text | 否 |  |  | 答案。 |
| status | varchar(32) | 否 | 'enabled' |  | QA 状态，常用 enabled、disabled、draft。 |
| source_document_id | uuid | 是 |  | 外键 documents(id) | 来源文档 ID。 |
| source_chunk_ids | uuid[] | 是 |  |  | 来源切片 ID 数组。 |
| source_url | text | 是 |  |  | 外部来源 URL。 |
| tags | text[] | 是 |  |  | 标签数组。 |
| version | integer | 否 | 1 |  | 版本号。 |
| created_by | uuid | 是 |  |  | 创建人管理员 ID。 |
| updated_by | uuid | 是 |  |  | 更新人管理员 ID。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |
| updated_at | timestamptz | 否 | now() |  | 更新时间。 |
| deleted_at | timestamptz | 是 |  |  | 软删除时间，非空表示已删除。 |

索引：

| 索引定义 |
| --- |
| CREATE INDEX IF NOT EXISTS idx_qa_pairs_question_trgm ON qa_pairs USING GIN(question gin_trgm_ops); |

### 3.8 qa_pair_embeddings

QA 对向量表，使用 pgvector 存储 1024 维 embedding。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| qa_pair_id | uuid | 否 |  | 唯一；外键 qa_pairs(id) | QA 对 ID。 |
| embedding | vector(1024) | 否 |  |  | pgvector 向量。 |
| embedding_model | varchar(128) | 否 |  |  | 向量模型名称。 |
| content_hash | varchar(128) | 否 |  |  | 内容哈希。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |
| updated_at | timestamptz | 否 | now() |  | 更新时间。 |

索引：

| 索引定义 |
| --- |
| CREATE INDEX IF NOT EXISTS idx_qa_pair_embeddings_vector_hnsw ON qa_pair_embeddings USING hnsw (embedding vector_cosine_ops); |

### 3.9 conversations

聊天会话表。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| title | varchar(255) | 否 |  |  | 标题。 |
| created_by | uuid | 是 |  |  | 创建人管理员 ID。 |
| summary | text | 否 | '' |  | 会话摘要。 |
| message_count | integer | 否 | 0 |  | 消息数。 |
| last_message_at | timestamptz | 是 |  |  | 最后消息时间。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |
| updated_at | timestamptz | 否 | now() |  | 更新时间。 |
| deleted_at | timestamptz | 是 |  |  | 软删除时间，非空表示已删除。 |

索引：

无

### 3.10 conversation_messages

聊天消息表，保存问答内容、引用和检索追踪。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| conversation_id | uuid | 否 |  | 外键 conversations(id) | 会话 ID。 |
| role | varchar(32) | 否 |  |  | 消息角色。 |
| content | text | 否 |  |  | 文本内容。 |
| rewritten_query | text | 是 |  |  | 改写后的检索问题。 |
| retrieval_trace | jsonb | 否 | '{}' |  | 检索追踪 JSON。 |
| citations | jsonb | 否 | '[]' |  | 引用来源 JSON。 |
| suggested_questions | jsonb | 否 | '[]' |  | 推荐追问 JSON。 |
| latency_ms | integer | 是 |  |  | 耗时毫秒。 |
| model_name | varchar(128) | 是 |  |  | 模型名称。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |

索引：

无

### 3.11 retrieval_logs

检索与回答日志表，用于调试和评估 RAG 效果。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| conversation_id | uuid | 是 |  | 外键 conversations(id) | 会话 ID。 |
| message_id | uuid | 是 |  | 外键 conversation_messages(id) | 消息 ID。 |
| raw_query | text | 否 |  |  | 原始查询。 |
| rewritten_query | text | 是 |  |  | 改写后的检索问题。 |
| recall_results | jsonb | 否 | '[]' |  | 召回结果 JSON。 |
| rerank_results | jsonb | 否 | '[]' |  | 重排结果 JSON。 |
| final_context | jsonb | 否 | '[]' |  | 最终上下文 JSON。 |
| citations | jsonb | 否 | '[]' |  | 引用来源 JSON。 |
| suggested_questions | jsonb | 否 | '[]' |  | 推荐追问 JSON。 |
| answer | text | 是 |  |  | 答案。 |
| answer_quality | jsonb | 否 | '{}' |  | 回答质量 JSON。 |
| model_name | varchar(128) | 是 |  |  | 模型名称。 |
| embedding_model | varchar(128) | 是 |  |  | 向量模型名称。 |
| rerank_model | varchar(128) | 是 |  |  | 重排模型名称。 |
| latency_ms | integer | 是 |  |  | 耗时毫秒。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |

索引：

无

### 3.12 evaluation_cases

评测用例表。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| question | text | 否 |  |  | 问题。 |
| expected_answer | text | 是 |  |  | 期望答案或关键词。 |
| expected_document_ids | uuid[] | 是 |  |  | 期望命中文档 ID 数组。 |
| expected_chunk_ids | uuid[] | 是 |  |  | 期望命中切片 ID 数组。 |
| tags | text[] | 是 |  |  | 标签数组。 |
| difficulty | varchar(32) | 是 |  |  | 难度。 |
| is_active | boolean | 否 | true |  | 是否启用。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |
| updated_at | timestamptz | 否 | now() |  | 更新时间。 |

索引：

| 索引定义 |
| --- |
| CREATE INDEX IF NOT EXISTS idx_eval_cases_tags ON evaluation_cases USING GIN(tags); |

### 3.13 evaluation_runs

评测批次表。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| name | varchar(255) | 否 |  |  | 名称。 |
| config | jsonb | 否 | '{}' |  | 配置 JSON。 |
| metrics | jsonb | 否 | '{}' |  | 指标 JSON。 |
| status | varchar(32) | 否 | 'running' |  | 评测批次状态：running、succeeded、failed。 |
| started_at | timestamptz | 是 |  |  | 任务开始时间。 |
| finished_at | timestamptz | 是 |  |  | 任务结束时间。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |

索引：

无

### 3.14 evaluation_results

评测结果明细表。

| 字段 | 类型 | 允许为空 | 默认值 | 约束 | 说明 |
| --- | --- | --- | --- | --- | --- |
| id | uuid | 否 | uuid_generate_v4() | 主键 | 主键 UUID。 |
| run_id | uuid | 否 |  | 外键 evaluation_runs(id) | 评测批次 ID。 |
| case_id | uuid | 否 |  | 外键 evaluation_cases(id) | 评测用例 ID。 |
| answer | text | 是 |  |  | 答案。 |
| hit_top3 | boolean | 是 |  |  | Top3 是否命中。 |
| hit_top5 | boolean | 是 |  |  | Top5 是否命中。 |
| answer_score | numeric(5,2) | 是 |  |  | 答案得分。 |
| citation_score | numeric(5,2) | 是 |  |  | 引用得分。 |
| suggested_question_score | numeric(5,2) | 是 |  |  | 推荐追问得分。 |
| failure_reason | varchar(64) | 是 |  |  | 失败原因。 |
| trace | jsonb | 否 | '{}' |  | 追踪 JSON。 |
| created_at | timestamptz | 否 | now() |  | 创建时间。 |

索引：

无

## 4. 关键业务关系

- `documents` 是文档主表，`document_parse_results`、`document_chunks`、`chunk_embeddings` 均通过 `document_id` 关联文档。
- `document_parse_results.document_id` 唯一，一篇文档最多一条解析结果。
- `document_chunks.parent_chunk_id` 指向同表，用于父子切片关系。
- `chunk_embeddings.chunk_id` 唯一，一个切片最多一条 embedding。
- `qa_pairs` 可选关联 `documents`，`qa_pair_embeddings.qa_pair_id` 唯一。
- `conversations` 与 `conversation_messages` 是一对多关系。
- `retrieval_logs` 可选关联会话和消息，用于审计和调试。
- `evaluation_runs`、`evaluation_cases`、`evaluation_results` 构成评测批次、用例和结果明细关系。
