# 学校 RAG 智能问答系统 - 一期开发计划表

> **项目周期**：7周左右，以 RAG 精度验收达标为核心出口  
> **一期目标**：先把学校知识库问答的准确度、可追溯性和可维护性做好  
> **技术边界**：一期使用 PostgreSQL 作为业务数据库、全文检索库和向量数据库，向量能力使用 pgvector；Redis 用于缓存和短期任务状态；文件存储用于保存原始文件和解析产物  
> **版本**：v1.1 一期聚焦版  
> **文档更新时间**：2026-06-06

---

## 1. 一期建设原则

一期不追求复杂产品形态和重型基础设施，核心目标是把 RAG 链路调准、调稳、可验证。

| 原则 | 说明 |
|------|------|
| 准确度优先 | 解析、切片、召回、重排、上下文组织、答案引用是一期最重要的工作 |
| 前端够用即可 | 前端先做基础工作台：多轮对话、文件上传、处理状态、QA 问答对管理 |
| PostgreSQL 优先 | 业务数据、文档元数据、全文索引、向量索引、对话记录统一进入 PostgreSQL |
| 缓存和文件存储保留 | Redis 负责缓存、短期会话和任务状态；文件存储负责原始文件和解析产物 |
| 可调试可回溯 | 每次问答都要记录检索结果、重排结果、命中文档、引用来源、文档 URL 和失败原因 |
| 来源必须可打开 | 只要答案依据来自文档，回答末尾必须展示可点击的文档 URL，方便用户直接查看原始资料 |
| 小步验收 | 每个模块都要用测试集和 bad case 验证，不等到最后才看问答效果 |

---

## 2. 一期范围

### 2.1 一期必须完成

| 功能域 | 一期内容 | 验收重点 |
|--------|----------|----------|
| 多轮对话 | 新增对话、切换对话、删除对话、连续追问、流式或准流式回答、推荐追问 | 对话上下文能参与问题改写，回答有来源引用和文档 URL，并推荐 3 个用户可能继续关心的问题 |
| 文件上传与入库 | 上传文件、文件列表、文件详情、删除文件、重新解析、重新向量化 | 能清楚展示文件是否已解析、已切片、已向量化、已入库 |
| 缓存与文件存储 | Redis 缓存短期状态、检索结果或会话上下文；文件存储保存原始文件和解析后的中间产物 | 文件可追溯，任务状态查询稳定，重复请求不会造成明显性能浪费 |
| 文档解析 | 重点支持 PDF、Word、Excel、Markdown、TXT；PPT 和 OCR 作为一期增强项 | 标题、段落、表格、页码、章节路径尽量保留 |
| 文本切片 | 按语义边界切片，支持表格保护、父子切片、重叠窗口、元数据挂载 | 切片不破坏核心语义，可定位回原文来源 |
| 向量化 | 文本向量生成、向量写入 PostgreSQL pgvector、重复内容缓存 | 相同内容不重复向量化，向量状态可追踪 |
| 混合召回 | pgvector 语义召回 + PostgreSQL 全文检索 + QA 问答对召回 | Top3、Top5 命中率达标 |
| 重排检索 | 对召回结果进行 rerank，过滤低相关内容，组织最终上下文 | 最终进入大模型的内容高相关、少噪声 |
| QA 问答对管理 | 从文档生成 QA、人工新增、修改、删除、启用、停用 | QA 对可以参与检索，也可以作为评测样本来源 |
| 问答生成 | 基于检索上下文回答，末尾展示参考来源和文档 URL；证据不足时不强答 | 降低幻觉，答案可追溯、来源可打开 |
| 推荐追问 | 根据当前问题、回答内容、引用文档和会话上下文生成 3 个推荐问题 | 推荐问题可点击发起下一轮对话，不脱离当前资料依据 |
| 精度评测 | 构建学校场景测试集，记录 bad case，持续调切片、召回、重排参数 | 以测试集结果作为一期验收依据 |

### 2.2 一期暂不做或弱化

| 内容 | 一期处理方式 |
|------|--------------|
| Milvus、ES 等额外检索数据库 | 暂不引入，降低部署和联调复杂度 |
| 高并发、集群、高可用部署 | 一期只保证小规模试点稳定可用 |
| PC + 移动端完整双端适配 | 一期以前端基础工作台为主，移动端不作为重点 |
| 复杂用户权限体系 | 先做基础账号或管理员入口，细粒度权限后续迭代 |
| GraphRAG、Agent 工作流、多模态复杂能力 | 放到二期，避免分散 RAG 基础准确度打磨 |
| 大规模运营监控看板 | 一期保留日志和关键链路记录，复杂监控后续补齐 |

---

## 3. 一期技术架构

### 3.1 核心组件

| 组件 | 一期方案 | 说明 |
|------|----------|------|
| 业务数据库 | PostgreSQL | 存储用户、文档、任务、切片、QA、对话、评测数据 |
| 向量数据库 | PostgreSQL + pgvector | 切片向量直接存入 PostgreSQL，使用 ivfflat 或 hnsw 索引 |
| 全文检索 | PostgreSQL 全文检索、pg_trgm 或应用层 BM25 | 用于关键词、专有名词、政策编号、部门名称等精确匹配 |
| 缓存 | Redis | 缓存短期会话、任务进度、热点 QA、检索临时结果和限流计数 |
| 文件存储 | MinIO 或本地文件存储 + PostgreSQL 元数据 | 保存原始文件、解析后的 Markdown/文本和必要中间产物；PostgreSQL 只保存路径、哈希和元数据 |
| 任务处理 | PostgreSQL 任务表 + Redis 状态缓存 + 后端后台任务 | PostgreSQL 负责任务持久化，Redis 负责实时进度和短期状态查询 |
| 文档解析 | 按文件类型接入解析器 | 重点关注解析质量和结构保留，而不是堆叠工具数量 |
| Embedding | 可配置 embedding 模型 | 记录模型名称、维度、版本，便于后续回放和重建索引 |
| Rerank | 可配置重排模型 | 对 TopN 召回结果重排，控制进入最终上下文的内容质量 |
| 大模型问答 | 可配置问答模型 | 只允许基于检索上下文回答，证据不足时返回无法确认 |

### 3.2 建议数据表

| 表名 | 主要用途 | 关键字段 |
|------|----------|----------|
| documents | 文档主表 | id、title、file_name、file_hash、file_path、source_url、preview_url、download_url、status、created_at |
| document_jobs | 文档处理任务 | id、document_id、job_type、status、progress、error_message、started_at、finished_at |
| document_parse_results | 解析结果 | document_id、content_md、content_text、parse_meta、quality_score |
| document_chunks | 文档切片 | id、document_id、chunk_no、content、chunk_type、page_no、section_path、metadata |
| chunk_embeddings | 切片向量 | chunk_id、embedding、embedding_model、content_hash、created_at |
| qa_pairs | QA 问答对 | id、question、answer、source_document_id、source_chunk_ids、status、tags、version |
| conversations | 对话会话 | id、title、created_at、updated_at |
| conversation_messages | 对话消息 | id、conversation_id、role、content、retrieval_trace、suggested_questions、created_at |
| retrieval_logs | 检索日志 | query、rewrite_query、recall_results、rerank_results、final_context、answer_quality |
| evaluation_cases | 评测样本 | question、expected_answer、expected_doc_ids、expected_chunk_ids、tags |

---

## 4. RAG 准确度建设重点

### 4.1 文档解析

| 重点 | 实现要求 |
|------|----------|
| 结构保留 | 保留标题层级、段落、页码、表格、列表、附件说明等信息 |
| 表格解析 | 表格不要直接压平成难以检索的文本，需补充表头、行列关系和上下文说明 |
| 清洗去噪 | 去除页眉页脚、页码、重复目录、无意义空白、乱码和重复段落 |
| 元数据提取 | 提取文档类型、部门、适用对象、发布日期、章节路径、页码 |
| 质量评分 | 对解析结果输出质量分，低质量文档进入人工确认或重新解析 |
| 可预览 | 前端可以查看解析后的 Markdown 或纯文本，便于人工发现解析问题 |

### 4.2 文本切片

| 重点 | 实现要求 |
|------|----------|
| 语义边界 | 优先按章节、段落、条款、句子切分，避免把一个完整政策条款切断 |
| 父子切片 | 保留父级章节摘要，子切片用于精确召回，父级内容用于补充上下文 |
| 表格保护 | 小表整体保留，长表按行组切分，并在每个切片中补充表头和表格标题 |
| 元数据增强 | 每个切片挂载文档名、页码、章节路径、文档类型、适用对象等信息 |
| 重叠策略 | 对长段落设置合理 overlap，减少边界处信息丢失 |
| 可调参数 | chunk size、overlap、TopK、重排阈值都要配置化，便于 bad case 调优 |

### 4.3 召回、重排与检索

| 重点 | 实现要求 |
|------|----------|
| 查询改写 | 多轮追问时补全指代，把口语问题改写成更适合检索的查询 |
| 语义召回 | 使用 pgvector 做向量召回，覆盖同义表达和泛化问题 |
| 关键词召回 | 使用 PostgreSQL 全文检索或关键词策略，覆盖政策编号、专业名、部门名、人名等精确词 |
| QA 召回 | 已维护的 QA 问答对作为高优先级召回来源，适合高频标准问答 |
| 融合排序 | 使用 RRF 或加权融合合并多路召回，避免单一路径漏召 |
| Rerank | 对 TopN 候选结果重排，保留最相关的 TopK 进入上下文 |
| 阈值过滤 | 低相关内容不进入答案生成，必要时返回“资料中未找到明确依据” |
| 引用来源 | 回答必须展示来源文档、章节、页码或切片位置，并在末尾给出可点击的文档 URL |
| 检索日志 | 保存 query、改写 query、召回列表、重排分数、最终上下文，便于复盘 |

### 4.4 答案来源 URL 展示

| 重点 | 实现要求 |
|------|----------|
| 文档 URL 入库 | 上传文件时生成 preview_url/download_url；如果文档本身来自外部链接，同时保存 source_url |
| 来源选择规则 | 有 source_url 时优先展示 source_url；没有外部链接时展示系统内部 preview_url 或 download_url |
| 回答末尾格式 | 回答结束后统一追加“参考来源”，列出文档名、章节/页码、URL |
| 多来源去重 | 多个 chunk 来自同一文档时合并展示，避免重复刷屏 |
| 权限控制 | 内部 URL 必须经过鉴权或签名，避免未授权访问原始文件 |
| 前端交互 | 来源 URL 可点击打开，支持跳转到文档预览页或下载原始文件 |
| 日志记录 | retrieval_logs 记录最终引用的 document_id、chunk_id、page_no、source_url/preview_url |

### 4.5 推荐追问生成

| 重点 | 实现要求 |
|------|----------|
| 生成时机 | 每次回答完成后生成 3 个推荐追问，随回答一起返回前端 |
| 生成依据 | 基于用户原问题、最终答案、引用文档、命中 chunk 和最近会话上下文生成 |
| 相关性控制 | 推荐问题必须围绕当前主题、政策细节、办理流程、条件限制或相关材料展开 |
| 证据约束 | 优先推荐当前知识库中有明确资料依据的问题，避免生成超出资料范围的问题 |
| 去重控制 | 推荐问题之间不能语义重复，也不能简单复述用户刚问过的问题 |
| 前端交互 | 推荐问题以按钮或列表形式展示，用户点击后直接作为下一轮问题发送 |
| 日志记录 | conversation_messages 或 retrieval_logs 记录 suggested_questions，便于评估推荐质量 |

---

## 5. 详细开发计划

### 阶段一：评测基线与技术边界确认

**周期**：0.5周  
**目标**：先确定一期验收怎么评，避免后期只凭主观感觉判断准确度。

| 任务ID | 任务名称 | 负责人 | 交付物 | 验收标准 |
|--------|----------|--------|--------|----------|
| P1-01 | 收集一期知识库样本文档 | 产品+测试 | 样本文档清单 | 覆盖学生常问政策、流程、通知、表格类资料 |
| P1-02 | 构建 RAG 评测问题集 | 产品+测试+算法 | 不少于100条评测问题 | 每条问题标注期望答案、来源文档或来源切片 |
| P1-03 | 定义精度指标 | 算法+项目经理 | 验收指标文档 | 明确 Top3、Top5 命中率、答案正确率、引用正确率 |
| P1-04 | 确认一期技术边界 | 后端+算法 | 技术方案说明 | 明确 PostgreSQL/pgvector 作为唯一数据库和向量数据库，Redis 和文件存储只承担缓存及文件保存职责 |

**阶段里程碑**：评测集和技术边界确认完成，可以开始按准确度目标开发。

---

### 阶段二：PostgreSQL 基础与文件入库

**周期**：0.75周  
**目标**：完成 PostgreSQL、pgvector、文档管理和处理任务底座。

| 任务ID | 任务名称 | 负责人 | 交付物 | 验收标准 |
|--------|----------|--------|--------|----------|
| P2-01 | PostgreSQL 与 pgvector 初始化 | 后端 | 初始化 SQL、迁移脚本 | pgvector 可正常写入和检索向量 |
| P2-02 | Redis 缓存初始化 | 后端 | Redis 配置和连接模块 | 可缓存任务状态、短期会话和热点查询结果 |
| P2-03 | 文件存储初始化 | 后端+运维 | MinIO 或本地文件存储配置 | 文件可上传、读取、删除，路径、哈希、预览 URL 和下载 URL 进入 PostgreSQL |
| P2-04 | 核心数据表设计 | 后端 | 表结构和索引 | documents、chunks、embeddings、qa_pairs、conversations 等表可用 |
| P2-05 | 文件上传与入库 | 后端 | 上传接口和存储目录规范 | 文件可上传，元数据进入 PostgreSQL，原始文件进入文件存储，并生成可访问 URL |
| P2-06 | 文档处理任务表 | 后端 | document_jobs 实现 | 支持解析、切片、向量化任务状态流转 |
| P2-07 | 文档状态展示接口 | 后端 | 文档列表和详情 API | 能展示未处理、解析中、已解析、已切片、已向量化、失败 |
| P2-08 | 文档 CRUD 接口 | 后端 | 文档管理 API | 支持查询、删除、重新解析、重新向量化 |

**阶段里程碑**：文件能上传入库，处理状态能被前端查询。

---

### 阶段三：文档解析与清洗

**周期**：1.25周  
**目标**：把学校文档稳定解析成适合切片和检索的结构化文本。

| 任务ID | 任务名称 | 负责人 | 交付物 | 验收标准 |
|--------|----------|--------|--------|----------|
| P3-01 | PDF 文本解析 | 算法+后端 | PDF 解析模块 | 文本型 PDF 可解析标题、段落、页码 |
| P3-02 | Word/Markdown/TXT 解析 | 算法+后端 | 通用文本解析模块 | 常见文档格式解析稳定 |
| P3-03 | Excel 表格解析 | 算法+后端 | 表格解析模块 | 表头、行列关系、合并单元格上下文尽量保留 |
| P3-04 | 解析结果标准化 | 算法 | Markdown/结构化文本输出 | 不同格式统一输出为可切片结构 |
| P3-05 | 文本清洗与去噪 | 算法 | 清洗规则 | 页眉页脚、目录重复、空白和乱码得到处理 |
| P3-06 | 解析质量评分 | 算法+测试 | 质量评分逻辑 | 低质量解析结果能被标记并提示人工复核 |
| P3-07 | 解析结果预览接口 | 后端 | 预览 API | 前端可查看解析后的 Markdown 或纯文本 |

**阶段里程碑**：样本文档解析可用，关键政策条款、表格和页码可追溯。

---

### 阶段四：切片、向量化与索引

**周期**：1周  
**目标**：形成高质量 chunk，并把全文索引和向量索引都写入 PostgreSQL。

| 任务ID | 任务名称 | 负责人 | 交付物 | 验收标准 |
|--------|----------|--------|--------|----------|
| P4-01 | 语义切片策略实现 | 算法 | 切片模块 | 优先按章节、条款、段落切片，不硬切语义 |
| P4-02 | 表格切片策略 | 算法 | 表格 chunk 规则 | 表格标题、表头、关键行列信息完整进入 chunk |
| P4-03 | 父子切片与元数据挂载 | 算法+后端 | chunk 元数据规范 | 每个 chunk 可定位文档、章节、页码 |
| P4-04 | Embedding 生成 | 算法 | 向量化模块 | chunk 可批量向量化，失败可重试 |
| P4-05 | 向量写入 pgvector | 后端 | chunk_embeddings 表和索引 | 支持相似度查询，向量维度和模型版本可追踪 |
| P4-06 | PostgreSQL 全文索引 | 后端 | 全文检索索引 | 支持关键词和专有名词召回 |
| P4-07 | 重复内容缓存 | 后端 | content_hash 机制 | 相同 chunk 不重复向量化 |
| P4-08 | 切片预览与重跑 | 后端+前端 | 切片预览接口 | 可查看 chunk 内容，并支持重新切片、重新向量化 |

**阶段里程碑**：文档完成解析、切片、向量化、全文索引和向量索引入库。

---

### 阶段五：召回、重排与问答链路

**周期**：1.5周  
**目标**：集中打磨 RAG 最核心的准确度能力。

| 任务ID | 任务名称 | 负责人 | 交付物 | 验收标准 |
|--------|----------|--------|--------|----------|
| P5-01 | 多轮查询改写 | 算法 | 查询改写模块 | 连续追问时能补全上下文和指代 |
| P5-02 | pgvector 语义召回 | 算法+后端 | 向量召回模块 | 支持 TopK 召回和元数据过滤 |
| P5-03 | PostgreSQL 关键词召回 | 算法+后端 | 全文召回模块 | 政策编号、部门名、专业名等精确词能命中 |
| P5-04 | QA 问答对召回 | 算法+后端 | QA 召回模块 | 已维护 QA 对可优先参与候选结果 |
| P5-05 | 多路召回融合 | 算法 | RRF 或加权融合模块 | 合并语义、关键词、QA 多路结果，减少漏召 |
| P5-06 | Rerank 重排 | 算法 | 重排模块 | TopN 候选能按相关性重新排序 |
| P5-07 | 相关性阈值过滤 | 算法 | 阈值策略 | 低相关结果不进入最终上下文 |
| P5-08 | 上下文组织 | 算法 | context packing 逻辑 | 控制上下文长度，优先保留高相关和同来源连续内容 |
| P5-09 | 答案生成与引用 URL | 算法+后端 | 问答链路 | 回答末尾带来源文档、章节、页码或 chunk 引用，并给出可点击文档 URL |
| P5-10 | 推荐追问生成 | 算法+后端 | 推荐问题生成模块 | 每次回答后返回 3 个相关追问，问题不重复且不脱离资料依据 |
| P5-11 | 无依据回答控制 | 算法 | 拒答策略 | 检索证据不足时不编造答案 |
| P5-12 | 检索日志记录 | 后端 | retrieval_logs | 每次问答可回看召回、重排、上下文、最终答案和推荐追问 |

**阶段里程碑**：100条评测问题的 Top5 命中率和答案正确率达到一期验收线。

---

### 阶段六：前端基础工作台与 API 联调

**周期**：1周  
**目标**：前端只做支撑 RAG 验证和基础使用的必要功能。

| 任务ID | 任务名称 | 负责人 | 交付物 | 验收标准 |
|--------|----------|--------|--------|----------|
| P6-01 | 前端项目基础搭建 | 前端 | 前端工程 | 可访问基础工作台 |
| P6-02 | 多轮对话窗口 | 前端 | 对话页面 | 支持新增对话、发送问题、查看回答、来源引用、文档 URL 和 3 个推荐追问 |
| P6-03 | 会话列表管理 | 前端+后端 | 会话 API 和侧栏 | 支持查看、切换、删除历史会话 |
| P6-04 | 文件上传页面 | 前端+后端 | 文件上传组件 | 支持上传文件并查看入库状态 |
| P6-05 | 文档状态列表 | 前端 | 文档管理页面 | 展示解析、切片、向量化、入库、失败状态 |
| P6-06 | 文档操作入口 | 前端+后端 | 操作按钮 | 支持删除、重新解析、重新切片、重新向量化 |
| P6-07 | QA 问答对管理 | 前端+后端 | QA 管理页面 | 支持生成、增加、修改、删除、启用、停用 |
| P6-08 | 检索调试视图 | 前端+后端 | 调试面板 | 可查看召回结果、重排分数、引用来源、最终展示 URL 和推荐追问生成依据 |

**阶段里程碑**：可以通过前端完成上传、入库、向量化、问答、QA 管理和检索调试。

---

### 阶段七：精度调优、测试与一期验收

**周期**：1周  
**目标**：围绕评测集和 bad case 调优，确保一期交付重点达标。

| 任务ID | 任务名称 | 负责人 | 交付物 | 验收标准 |
|--------|----------|--------|--------|----------|
| P7-01 | 全链路功能测试 | 测试 | 功能测试报告 | 上传、解析、切片、向量化、检索、问答流程无阻断问题 |
| P7-02 | 检索命中率评测 | 测试+算法 | 检索评测报告 | Top3、Top5 命中率达到验收标准 |
| P7-03 | 答案质量人工评测 | 测试+产品 | 人工评测报告 | 答案正确性、完整性、引用准确性达标 |
| P7-04 | bad case 复盘 | 算法+后端 | bad case 清单 | 每个失败样本标注原因：解析、切片、召回、重排或生成 |
| P7-05 | 参数调优 | 算法 | 调优记录 | chunk size、overlap、TopK、融合权重、rerank 阈值有记录 |
| P7-06 | QA 问答对补充 | 产品+测试 | 高频 QA 对 | 高频问题有人工确认的标准问答 |
| P7-07 | 一期验收回归 | 全体 | 验收报告 | 核心指标达标，遗留问题进入二期列表 |

**阶段里程碑**：RAG 准确度达到一期验收标准，系统可进入小范围试点。

---

## 6. 一期甘特图概览

```text
第1周  评测基线确认 + PostgreSQL/pgvector + 文件入库基础
第2周  文档解析、结构保留、清洗去噪
第3周  解析完善 + 语义切片 + 表格切片
第4周  向量化入库 + 全文索引 + pgvector 检索
第5周  混合召回 + Rerank 重排 + 上下文组织 + 引用回答
第6周  前端基础工作台 + API 联调 + QA 问答对管理
第7周  精度评测 + bad case 调优 + 回归测试 + 一期验收
```

---

## 7. 一期验收标准

| 验收项 | 指标要求 | 测试方法 |
|--------|----------|----------|
| 文档处理链路 | 上传、解析、切片、向量化、入库状态完整可见 | 上传样本文档并检查状态流转 |
| 解析可用率 | 样本文档中 95% 可得到可检索文本 | 人工抽查解析结果和原文一致性 |
| 表格信息保留 | 关键表格信息保留率不低于 90% | 抽查表格类问题能否命中正确表格 |
| 检索 Top3 命中率 | 不低于 85% | 使用标注评测集验证来源命中 |
| 检索 Top5 命中率 | 不低于 90% | 使用标注评测集验证来源命中 |
| 答案正确率 | 不低于 85% | 人工评估答案是否符合资料依据 |
| 引用正确率 | 不低于 90% | 检查引用文档、章节、页码或 chunk 是否正确 |
| 来源 URL 可用率 | 不低于 95% | 检查回答末尾的文档 URL 是否可打开，且指向正确文档 |
| 推荐追问可用率 | 每次回答稳定返回 3 个问题，相关可点击率不低于 90% | 人工抽查推荐问题是否相关、非重复、可继续追问 |
| 无依据回答控制 | 明显无依据问题的强答率不高于 5% | 使用反向问题和超范围问题测试 |
| QA 问答对管理 | 支持生成、新增、修改、删除、启用、停用 | 前端操作验收 |
| 检索可调试性 | 每次问答可查看召回、重排、最终上下文 | 检查 retrieval_logs 和前端调试视图 |

---

## 8. 关键风险与应对

| 风险点 | 影响 | 应对策略 | 负责人 |
|--------|------|----------|--------|
| 文档解析质量差 | 后续切片和召回都会失准 | 增加解析质量评分和人工预览，优先修复高频资料类型 | 算法 |
| 切片过粗或过细 | 召回噪声大或答案缺上下文 | 建立 bad case 调参机制，记录 chunk 参数版本 | 算法 |
| 只用 PostgreSQL 后检索性能不足 | 影响候选召回速度 | 控制一期数据规模，优化 pgvector 和全文索引，必要时分批检索 | 后端 |
| 关键词和语义召回互相漏召 | 影响 TopK 命中率 | 使用混合召回和融合排序，不依赖单一路径 | 算法 |
| Rerank 阈值不稳定 | 可能误删正确答案或放入噪声 | 阈值按评测集调优，并保留重排日志复盘 | 算法 |
| 大模型编造答案 | 降低可信度 | 强制基于上下文回答，证据不足时拒答，并展示引用和文档 URL | 算法 |
| QA 自动生成质量不稳定 | 错误 QA 会污染检索 | 自动生成后必须支持人工审核、修改、启用和停用 | 产品+测试 |

---

## 9. 二期候选内容

一期验收后，再根据试点结果决定是否进入以下能力建设：

| 方向 | 内容 |
|------|------|
| 基础设施增强 | Milvus 或 ES、生产级监控告警、文件存储高可用和备份策略 |
| 权限体系 | 多角色、多部门、多知识库权限隔离 |
| 前端体验 | 移动端适配、个人中心、反馈闭环、运营看板 |
| 高级 RAG | GraphRAG、复杂 Agent 工作流、跨文档推理、自动纠错学习 |
| 文档能力 | OCR、图片理解、PPT 深度解析、复杂表格校验 |
| 性能规模化 | 大规模知识库、多校区、多租户、高并发压测和扩容 |

---

## 10. 一期交付清单

| 类型 | 交付物 |
|------|--------|
| 后端 | 文档管理 API、处理任务 API、切片向量化 API、问答 API、QA 管理 API、会话 API |
| 前端 | 多轮对话窗口、推荐追问入口、文档上传和状态页、QA 问答对管理页、检索调试视图 |
| 数据库与存储 | PostgreSQL 表结构、pgvector 索引、全文索引、Redis 缓存配置、文件存储配置、初始化和迁移脚本 |
| 算法 | 解析模块、切片模块、embedding 模块、混合召回模块、rerank 模块、答案生成策略 |
| 测试 | 评测问题集、检索命中率报告、答案质量报告、bad case 清单 |
| 文档 | 技术方案、接口文档、RAG 参数说明、一期验收报告 |
